name: DoOrDie

options:
  bundleIdPrefix: com.brolee
  deploymentTarget:
    iOS: "18.0"
  createIntermediateGroups: true
  groupSortPosition: top
  generateEmptyDirectories: true
  minimumXcodeGenVersion: "2.40.0"

settings:
  base:
    DEVELOPMENT_TEAM: PVQK77DQWL
    CODE_SIGN_STYLE: Automatic
    MARKETING_VERSION: "1.0"
    CURRENT_PROJECT_VERSION: "1"
    SWIFT_VERSION: "6.0"
    IPHONEOS_DEPLOYMENT_TARGET: "18.0"
    TARGETED_DEVICE_FAMILY: "1"
    ENABLE_USER_SCRIPT_SANDBOXING: "NO"
    GENERATE_INFOPLIST_FILE: "NO"
    SWIFT_EMIT_LOC_STRINGS: "YES"
    ENABLE_BITCODE: "NO"
    SWIFT_APPROACHABLE_CONCURRENCY: "YES"
    SWIFT_DEFAULT_ACTOR_ISOLATION: MainActor
    SWIFT_UPCOMING_FEATURE_INFER_SENDABLE_FROM_CAPTURES: "YES"

targets:
  DoOrDie:
    type: application
    platform: iOS
    sources:
      - path: App
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.brolee.app
        PRODUCT_NAME: DoOrDie
        INFOPLIST_FILE: App/Info.plist
        CODE_SIGN_ENTITLEMENTS: App/DoOrDie.entitlements
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor
        ENABLE_PREVIEWS: "YES"
        SUPPORTS_MACCATALYST: "NO"
    dependencies:
      - target: DoOrDieWidgets

  DoOrDieWidgets:
    type: app-extension
    platform: iOS
    sources:
      - path: Widgets
      - path: App/Models
      - path: App/Persistence
      - path: App/DesignSystem/Theme.swift
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.brolee.app.widgets
        PRODUCT_NAME: DoOrDieWidgets
        INFOPLIST_FILE: Widgets/Info.plist
        CODE_SIGN_ENTITLEMENTS: Widgets/DoOrDieWidgets.entitlements
        ENABLE_PREVIEWS: "YES"
        SUPPORTS_MACCATALYST: "NO"

schemes:
  DoOrDie:
    build:
      targets:
        DoOrDie: all
        DoOrDieWidgets: all
    run:
      config: Debug
    profile:
      config: Release
    archive:
      config: Release
